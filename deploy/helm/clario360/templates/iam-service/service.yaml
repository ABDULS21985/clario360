{{- if .Values.iamService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "clario360.fullname" . }}-iam-service
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.componentLabels" (dict "root" . "component" "iam-service") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8081
      targetPort: http
      protocol: TCP
    - name: metrics
      port: 9091
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "clario360.componentSelectorLabels" (dict "root" . "component" "iam-service") | nindent 4 }}
{{- end }}
