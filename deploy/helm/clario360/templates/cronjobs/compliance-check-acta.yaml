{{- if .Values.cronjobs.complianceCheckActa.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "clario360.fullname" . }}-compliance-check-acta
  namespace: {{ include "clario360.namespace" . }}
  labels:
    {{- include "clario360.labels" . | nindent 4 }}
    app.kubernetes.io/component: cronjob-compliance-check-acta
spec:
  schedule: {{ .Values.cronjobs.complianceCheckActa.schedule | quote }}
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  successfulJobsHistoryLimit: 3
  startingDeadlineSeconds: 300
  jobTemplate:
    spec:
      activeDeadlineSeconds: {{ .Values.cronjobs.complianceCheckActa.activeDeadlineSeconds | default 600 }}
      backoffLimit: {{ .Values.cronjobs.complianceCheckActa.backoffLimit | default 2 }}
      template:
        metadata:
          labels:
            app.kubernetes.io/component: cronjob-compliance-check-acta
            app.kubernetes.io/part-of: clario360
        spec:
          restartPolicy: OnFailure
          {{- with .Values.global.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            fsGroup: 65534
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: compliance-check-acta
              {{- if .Values.cronjobs.complianceCheckActa.image }}
              image: "{{ .Values.global.imageRegistry }}/{{ .Values.cronjobs.complianceCheckActa.image.repository }}:{{ .Values.cronjobs.complianceCheckActa.image.tag | default .Chart.AppVersion }}"
              {{- else }}
              image: "{{ .Values.global.imageRegistry }}/{{ .Values.actaService.image.repository }}:{{ .Values.actaService.image.tag | default .Chart.AppVersion }}"
              {{- end }}
              imagePullPolicy: {{ .Values.global.imagePullPolicy | default "IfNotPresent" }}
              command: ["/app"]
              args: ["--job=compliance-check-acta"]
              env:
                - name: ACTA_DB_URL
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "clario360.fullname" . }}-database-credentials
                      key: ACTA_DB_URL
                - name: ACTA_REDIS_ADDR
                  valueFrom:
                    configMapKeyRef:
                      name: {{ include "clario360.fullname" . }}-shared
                      key: REDIS_HOST
                - name: REDIS_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.redis.existingSecret }}
                      key: REDIS_PASSWORD
                - name: ACTA_KAFKA_BROKERS
                  valueFrom:
                    configMapKeyRef:
                      name: {{ include "clario360.fullname" . }}-shared
                      key: KAFKA_BROKERS
                - name: OTEL_SERVICE_NAME
                  value: "compliance-check-acta"
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop:
                    - ALL
              resources:
                {{- toYaml .Values.cronjobs.complianceCheckActa.resources | nindent 16 }}
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir: {}
{{- end }}
